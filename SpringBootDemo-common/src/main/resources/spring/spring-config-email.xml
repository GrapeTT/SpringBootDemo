<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    
    <bean id="emailClient" class="com.demo.email.EmailClient">
        <property name="host" value="${springbootdemo.email.host}"/>
        <property name="port" value="${springbootdemo.email.port}"/>
        <property name="sender" value="${springbootdemo.email.sender}"/>
        <property name="isAuth" value="${springbootdemo.email.isAuth}"/>
        <property name="authCode" value="${springbootdemo.email.authCode}"/>
        <property name="title" value="SpringBootDemo：验证码" />
    </bean>
</beans>